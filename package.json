{
  "name": "build-reactor-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run-dev": "node -e 'require(\"./index.js\")()'",
    "run-output": "node -e 'require(\"./output.js\")()'",
    "build": "esbuild index.js --bundle --platform=node --minify --tree-shaking=true --outfile=output.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-emoji": "^1.11.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.5"
  }
}
